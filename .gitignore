.DS_Store

git/gitconfig-work
git/gitconfig-local

zsh/local.zsh

# Vendored remote nvim plugins (populated by `make ssh-remote-vendor`)
ssh-remote/bundle/xdg/config/nvim/pack/

.claude/

# Agent planning artefacts. Never publish specs, plans or review diffs.
.superpowers/
docs/superpowers/

# Machine-specific software. The tracked manifests hold the shared baseline only.
Brewfile.local
setup/cask-apps.local.tsv
setup/manual-checks.local.tsv
